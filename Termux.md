Установить расширения для доступа к директории `storage` с файлами телефона:

```shell
pkg update
pkg install termux-api
termux-setup-storage
```

gpg and ssh keys: [[Что надо сделать при настройке нового рабочего места]]