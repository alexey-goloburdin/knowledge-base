# Вопросы про ИИ для самых маленьких

Уооо здоров котаны! Благодаря комментариям под последним видео удалось получить срез понимания темы искусственного интеллекта в обществе. Давайте немножко поднимем этот уровень! Вопросы про ИИ для самых маленьких. Заблуждения, мифы, тайны, интриги.

Погнали!

Итак, от простого к сложному, для начала надо разобраться в терминологии. Как было выяснено, для многих людей LLM и агент (==покажи 2 слова==) — это одно и тоже, всё какой-то чёрный ящик, который творит магию. Многие реально не понимают, что Claude Sonnet и Claude Code это **разные** вещи (==покажи==).

Это очень важно, давайте разберёмся!

Начнём с того, что такое LLM и как она работает.

# LLM — просто очень длинная математическая функция

LLM (Large Language Model, ==покажи==), большая языковая модель — это математическая функция с миллиардами параметров внутри, эти параметры подбираются автоматически на огромных массивах текстов в процессе обучением модели. Модель находит закономерности в языке и предсказывает, как продолжить текст. По сути это очень умное автодополнение текста.

Какие есть LLM? Есть множество моделей от ChatGPT, например, GPT-5.4 или GPT-5.4-mini (==покажи== https://developers.openai.com/api/docs/models), есть модели от Antropic (==покажи== https://platform.claude.com/docs/en/about-claude/models/overview), например, claude-opus-4-6, claude-sonnet-4-6, claude-haiku-4-5 и тд. Это модели, которые развернуты в облаке, и вы за определенную плату получаете к ним доступ.

При этом есть множество моделей, которые вы можете легально бесплатно установить на свой компьютер или даже на телефон и использовать их затем полностью локально без интернета и без оплаты за использование. Например, модели от китайской компании Alibaba, это модели qwen (==покажи== https://huggingface.co/collections/Qwen/qwen35), скажем, qwen3.5-35b-a3b, qwen3.5-9b, или гугловые модели, например, Gemma 4.

То есть существует множество больших языковых моделей, бесплатные, платные, которые работают в облаке, которые работают локально, но каждая из этих больших языковых моделей это просто математическая формула, и задача этой математической формулы это предсказание продолжения текста.

Давайте здесь немного углубимся и поговорим о том, как работает большая языковая модель, вот на уровне школьной математики, это очень интересно.

Итак, вы задаете какой-то текстовый запрос модели, например, *зачем учить программирование в 2026 году?* (покажи в интерфейсе claude). Этот запрос называют промптом, иии это текст, а LLM это математическая формула, которая работает с числами. Поэтому надо как-то текст перевести в числа. Как это происходит? Это происходит с помощью токенизатора. Например, вот демонстрация работы токенизатора OpenAI https://platform.openai.com/tokenizer (покажи с запросом *зачем учить программирование в 2026 году?*).

Как видим, в этом промпте 6 слов плюс знак вопроса, но в токенах это получается 12 токенов. Почему какие-то слова это один токен, а какие-то несколько токенов? Потому что так эффективнее кодировать информацию из текста в числа. Часто встречающиеся последовательности символов представлены одним токеном, а редко встречающиеся несколькими токенами. При этом один и тот же токен может быть частью разных слов. Например, слова: *нового старого моднявого*, символы *ого* это один и тот же токен, как видим в токенизаторе.

Жмём Token IDs в интерфейсе токенизатора OpenAI и видим, что каждому токену нашего текста соответствует какое-то целое число. И это число это просто номер этого токена в словаре модели. Словарь модели это буквально все токены, то есть все последовательности символов, которые известны модели. Размер словаря порядка 250 тысяч элементов и по сути весь наш человеческий язык упакован в эти 250 тыс элементов, причём как английский, так и русский и какие-то другие популярные языки. Это очень интересно, я считаю. Всего 250 тысяч элементов кодируют все слова всех популярных языков.

Если бы мы каждое слово кодировали отдельным токеном, то общее количество возможных токенов было бы огромным, особенно с учётом русского языка с его склонениями и падежами, и словарь был бы огромным и оперировать модели таким огромным словарём было бы сложно и неэффективно. Поэтому токен это часто встречающийся набор символов, иногда составляющий всё слово, но чаще содержащий только часть слова.

Итак, мы ввели промпт и он побился по токенам, а каждый токен представлен просто порядковым числом в словаре модели. 

Эти числа и передаются на вход математической функции модели.

Идём дальше, есть понятие контекстного окна модели. Это максимальный объём текста, который языковая модель может обрабатывать. Если контекстное окно 4000 токенов, то мы не сможем передать в модель текст на 5000 токенов, потому что это превышает контекстное окно.

Так вот количество аргументов этой математической функции, которая представляет из себя LLM — это и есть ширина контекстного окна модели.

Вот есть у нас, например, функция (==покажи==) f(x) =3154x^2 + 5x + 216

У этой функции один аргумент `x`,  а числа 3154, 2, 5 и 216 это параметры этой функции. 

А вот у этой функции два аргумента — x1 и x2: f(x1, x2) =3154x1^2 + 5x2 + 216 * (x1 + x2)

Мы можем сделать функцию с тремя аргументами, или 10ю аргументами, или с тысячей, или с 4000. Вот у современных LLM-моделей ширина контекстного окна, составляет где-то 200 тысяч токенов, а где-то и миллион токенов. Это и есть то самое количество аргументов, которое можно передать на вход математической функции этой модели.

Например, наш промпт *зачем учить программирование в 2026 году?* это 12 токенов и в функцию аргументами передадутся эти 12 чисел и остальные 200 тысяч минус 12 аргументов передадутся пустыми.

Задача функции очень простая — предсказать следующий токен. Каждый вызов функции предсказывает один следующий токен текста. Но что именно возвращает функция? Функция возвращает тот самый один наиболее вероятный токен или как? На самом деле нет, функция всегда возвращает весь словарь токенов, то есть все, например, 250 тысяч токенов, известных модели, где каждому токену проставлена вероятность того, что именно этот токен следующий. И затем выбирается один из этих наиболее вероятных токенов, причём на это можно влиять, например, параметром температуры, который есть у API моделей. При температуре 0 результат будет более детерминирован, то есть более воспроизводим и стабилен, но будет менее творческим, потому что будут выбираться наиболее вероятные токены. При температуре ближе к единице наоборот будут чаще выбираться менее вероятные токены, результат будет менее воспроизводимым и детерминированным, но более творческим, более неожиданным.

Я тут использую слово детерминированный, может, не все его понимают. Детерминированный результат это предсказуемый и всегда воспроизводимый результат. Есть у нас функция f(x) = x^2, и если ей на вход передать 4, то она всегда вернёт 16, при любых условиях. Это детерминированная функция. А недетерминированная функция зависит от каких-то вероятностей, её результат не всегда одинаковый для одинаковых входных данных.

Так вот функция LLM недетерминирована, она вероятностная, мы не знаем, что вернёт модель, стопроцентной воспроизводимости ответов даже на температуре 0 нет. Это связано с разными параметрами, в частности с ошибками округления нецелых чисел и с несовершенством работы компьютера с нецелыми числами. Когда математические операции выполняются с нецелыми числами, то на результат может влиять даже порядок выполнения операций. Например:

```python
>>> 0.1 + 0.2 + 0.3
0.6000000000000001
>>> 0.3 + 0.2 + 0.1
0.6
```

А в видеокартах при математических расчётах порядок выполнения операций не всегда детерминирован, поэтому и результат тоже недетерминирован.

Итак, вернёмся к принципу работы LLM.  У нас есть промпт *зачем учить программирование в 2026 году?*, мы передали этот промпт в функцию и функция возвращает все 250 тысяч токенов своего словаря с проставленными вероятностями. Вот вероятность того, что следующий токен это слово «морковь» — маленькая, а вероятность того, что следующий токен это слово «хороший» — большая. И в итоге следующим словом берётся именно слово хороший. Вы уже понимаете, что токен это не то же самое, что слово, но для упрощения я говорю именно о словах, а не токенах, хотя модель работает именно с токенами.

Кстати, слово good это 1 токен, а слово хороший — это 3 токена. И такая закономерность в целом наблюдается для английского и русского текста. Так что если вы хотите эффективнее тратить токены, а API платных моделей тарифицируется именно за токены, то используйте английский язык, а не русский, потому что английский эффективнее по токенам, так уж вышло, один символ английского алфавита в кодировке utf8 это 1 байт, а русского алфавита 2 байта, плюс обучающих текстов на русском языке меньше, чем на английском, поэтому вот как есть. Английский язык по токенам эффективнее.

Иии вот так и работает LLM. Мы передали в математическую функцию входные 12 токенов, функция вернула вероятности для следующего токена и был выбран один следующий токен. Затем функция вызывается уже с 13 входными токенами, чтобы сгенерировать 14й токен. Затем функция вызывается уже с 14ю входными токенами, чтобы сгенерировать 15й токен и так далее.

А те параметры, которые есть внутри этой функции, подбираются автоматически в процессе обучения модели на огромном количестве текстов. Потом полученная модель, то есть полученная функция, ещё донастраивается, её параметры донастраиваются уже с человеком, с обратной связью от человека, и на выходе получается та функция, которой мы все пользуемся.

И которую называем искусственным интеллектом! Согласитесь, понимая это, очень интересно смотреть на рассуждения о сознании модели, о субъектности модели, о том, что модель захватит мир и так далее. Вот функция f(x) = x^2 уже готова захватить мир:) А LLM это такая же функция, просто более длинная и сложная, настолько длинная и сложная, что буквально никто в мире не понимает, как именно она работает на уровне того, почему при таких-то входах даётся такой-то выход, это невозможно отследить через эти миллиарды коэффициентов внутри функции, но это всё равно просто математическая функция и не более того.

Поговорим теперь про ИИ-агентов.

# ИИ-агенты

Итак, автодополнение текста это то, что делает большая языковая модель. Это даёт возможность початиться с моделькой, и это весело и увлекательно, но в общем и не так чтобы уж сильно полезно. Вот если бы дать модели *инструменты взаимодействия с внешним миром*... Тогда это может быть гораздо более полезным. Чтобы моделька имела доступ к вашему календарю, к интернет-поисковику, к документации вашего продукта, к вашему github или gitlab, к вашему умному дому и так далее и тому подобное.

Так вот ИИ-агенты как раз дают вам такие возможности. Что такое ИИ-агент? Это большая языковая модель плюс обычная детерминированная программа, которую вы можете написать на любом ЯП, например, на Python. Ваша программа в нужный момент может вызывать LLM, или LLM может вызывать ваш код, в зависимости то того, как вы всё настроите.

Например, мы хотим фильтровать спам-сообщения в нашем телеграм-чатике. Большие языковые модели, причём даже лёгкие бесплатные, которые вы можете запустить на своём ноутбуке, вполне отлично справляются с задачей классификации текста, то есть определения, например, является ли сообщение спамом. И вы пишете программу, которая читает все новые появляющиеся в чате сообщения и может их удалять, если посчитает спамом. Вот поступает новое сообщение в чат, программа его видит и обращается к LLM и просит LLM определить, является ли это сообщение спамом. Если LLM отвечает, что да, это с вероятностью 95% спам, то наша программа удаляет это сообщение.

И это вот ИИ-агент. То есть наша программа плюс LLM. LLM делает то, что она умеет делать хорошо, например, понимать язык, классифицировать текст, анализировать текст и тд, а наша программа делает то, что хорошо решает обычная детерминированная логика, например, написанная на ЯП Python. В этом примере именно наш код вызывает LLM.

Но может быть и обратная ситуация, мы можем передавать на вход нашей LLM так называемые tools, инструменты, по сути функции в нашем языке программирования, и сама модель может в своём ответе просить вызвать ту или иную функцию с какими-то аргументами. То есть сама модель принимает решение, когда и с какими аргументами надо вызвать функцию.

Например, мы сделали чат-бота для нашей компании, это та же самая LLM, но ей переданы инструкции промптом о том, что она должна делать, и переданы доступные ей инструменты, которые она может вызывать. Пользователь пишет в чат сообщение в духе «Забронируй мне поездку в Питер на следующую пятницу на такую-то конференцию, подбери отель рядом с местом конференции и добавь это в мой календарь» и модель, вызывая доступные ей инструменты, решает эту задачу. Перечень инструментов для этой задачи может быть следующим:

- `search_flights(city, date)` — функция, которая идёт в API авиакомпаний и возвращает список рейсов
- `search_hotels(city, location_query)` — функция, которая ищет отели по ключевым словам или по координатам
- `get_calendar_events(date)` — функция, читающая календарь пользователя, в котором забиты планируемые активности на ближайшие дни
- `create_calendar_event(details)` — функция, создающая запись в календаре
- `send_email(recipient, body)` — функция для отправки письма (например, подтверждения пользователю или менеджеру)

Как работает этот агент. Пользователь вводит запрос: «Забронируй мне поездку в Питер на следующую пятницу, подбери отель рядом с местом конференции и добавь это в мой календарь». LLM получает запрос и понимает: «Мне нужно знать, что у пользователя запланировано на эту пятницу». Для этого она не выдает текст пользователю, она возвращает специальный вызов функции: `call: get_calendar_events(date="2026-05-15")`, и наша программа читает этот ответ модели и понимает, что ей надо вызвать эту функцию и её результат передать в модель. Что наша программа и делает.

LLM видит результат функции и думает: «Так, конференция в 14:00. Значит, отель должен быть рядом с местом проведения конференции или в центре, чтобы пользователь успел». Она вызывает второй инструмент: `call: search_hotels(city="St. Petersburg", location_query="near conference center")`.

Эта функция, реализованная в нашей программе, возвращает список отелей. LLM выбирает лучший вариант (по цене или рейтингу) и решает, что пора завершать задачу. Она вызывает `call: create_calendar_event(...)` с деталями рейса и отеля, а затем делает финальный вызов `call: send_email(...)`.

После этого модель пишет пользователю понятным языком: _"Готово! Я нашел рейс в 10 утра, забронировал отель 'Астория' (он рядом с твоей конференцией) и добавил всё в календарь. Подтверждение отправил на почту"_.

И тут нет жесткого алгоритма `if-else`: мы не программировали агента на случай, если в календаре уже будет встреча на день конференции. Модель сама видит встречу и подстраивает стратегию поиска отеля. Агент при этом сам обрабатывает неопределённые ситуации, если в запросе не указана дата, агент может спросить пользователя: "На какую именно пятницу мы планируем?". Модель использует доступные ей инструменты как «руки», чтобы собирать информацию по кусочкам, анализировать её и добиваться в  конечном итоге поставленной цели.

Это и есть ИИ-агенты, то есть комбинация  твоего детерминированного кода с вероятностной моделью.


---


программа, написанная на каком-то языке программирования, например, Python, которая в процессе своей работы обращается к большой языковой модели, передавая её что-то на вход и как-то обрабатывая результат от неё. Причём как ваш код может в нужный момент вызывать LLM, так и LLM может в нужный ей момент вызывать ваш код, это как вы всё настроите.

Например, 




---



Надо просто выбрать самый крутую ИИ-модель на сегодня, самый новую, самую продвинутую, вышедшую желательно сегодняшней ночью, и тогда она вам всё сделает, запрограммирует любую систему, решит все ваши задачи, сделает жизнь счастливее и отдельные органы длиннее. Это не так и на протяжении видео вы поймёте, почему. Если коротко — на качество решения задачи в бо́льшей степени влияет качество агента, чем качество ИИ-модели. Дальше разберёмся с этим подробнее.

И тут мы переходим к заблуждению второму, которое заключается в том, что люди не различают понятий ИИ-модели и ИИ-агента . Итак, у нас тут вырисовывается три термина — ИИ, ИИ-модель и ИИ-агент.







Что такое ии и как он работает

Глюки везде (как борятся в агентах). Покажи на примере json с реквизитами? Перепроверка моделью в другим контекстом

Разница агента и ии

Хранит историю - в общем случае это ваша задача, ллм это перемножитель матриц, а не postgresql для хранения сообщений. Хотя какие-то апи ллм и хранят сообщения , но это просто в этих апр прикрутили к ллм сохранение контекста где-то в БД, вы можете это сделать с любой ллм и любым апи просто сами у себя локально, сохраняйте сообщения, то есть контекст и будет вам персистентность

Множитель скила для профессионала

Мвп делать удобно, но продакшн большая система не мвп, нужны спецы

Дорого, корпорации ищут способы экономии токенов

Долгое железо надо от 10к долларов

Прошел год, может модель поумнела? Ну давайте сравним на математической задаче перемножения чисел. Это оч простая операция, вы можете на калькуляторе ее всегда решить. Но модель не справляется в API. Почему? Да потому что она фундаментально не знает, сколько будет одно число умножить на другое, если этих данных не было в наборе данных, использованных в её обучении. Как бы большая языковая модель ни была хороша, она не избавляется от своей природы — это просто статистический вероятностный алгоритм, который ищет следующий токен, используя паттерны, то есть шаблоны, которые присутствовали в обучающих данных.

Вы можете сказать что в чате вот она считает  — да, потому что там уже не голая LLM, а по сути агент, который умеет вызывать некоторые встроенные тулы, например, тул расчётов или тул поиска в интернете. Это вызов детерменированного кода. Это и есть суть ИИ-агентов, что LLM может сама решать когда вызывать тулы, это очень круто, но это не делает модель способной решить те задачи, которые она принципиально решить не может. За это в частности критикуют LLM как что-то, что может прийти к AGI, например, Ян Лекун, авторитетный учёный в области ML, компьютерного зрения и тд. Кстати, он создатель формата DJVU, в котором у нас в универе были большинство книг, потому что сканы в DJVU занимают сильно меньше места, чем в PDF:)

Скаже о том, что во многом все зависит не от умности модели, а от промтов и выстроенного механизма работы с моделью. Покажи, как достает модель все реквизиты одним промптом, и что она делает это не очень хорошо. И покажи, что можно разбить на несколько промтов и добиваться результата. Например, это можно показать на Вордовском документе с реквизитами Райфайзенбанка, где есть сложности с определением Инн и Кпп.

модель не считает  (покажи на примере длинных чисел)

---

сделать дисклеймер о том, что ты не специалист в ML и нейросетях?