- [ ] SSH-keys
	- [ ] `cp -R ~/.ssh /Volumes/disk/ssh`
- [ ] GPG-keys
```shell
cd $HOME && \
   mkdir gpgkeys && \
   gpg --export-secret-key -a sterx > ./gpgkeys/secret_key.gpg && \
   gpg --export --armor -a sterx > ./gpgkeys/public.pub

cd gpgkeys
zip -e gpgkeys.zip public.pub secret_key.gpg
```
- [ ] софт:
	- [ ] git, nvim, htop, tmux, gcc, zip, unzip, git, curl
	- [ ] python3.13
	- [ ] postgresql 17
- [ ] GUI-софт:
	- [ ] chrome/chromium
	- [ ] obsidian
	- [ ] cyberduck or another
	- [ ] davinci resolve

import GPG keys:

```
gpg --import public.pub
gpg --import secret_key.gpg
```